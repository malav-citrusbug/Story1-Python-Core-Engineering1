import sys
import os

if sys.prefix==sys.base_prefix:
    print("Error")
    print("the script must be run in a python enviroment")
    sys.exit(1)
print("Done Nice To hear that you have made a virtual enviroment you're inside a virtual enviroment")
    
    